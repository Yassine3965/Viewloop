# ViewLoop Chrome Extension

إضافة Chrome لكسب النقاط من خلال مشاهدة الفيديوهات على يوتيوب.

## 🚀 الميزات

- ✅ **مراقبة الفيديوهات الآمنة**: تتبع المشاهدة مع منع التلاعب
- 🔐 **أمان متقدم**: نظام سر ديناميكي وتشفير
- 💾 **تخزين ذكي**: حفظ التوكن محلياً وعبر الإنترنت
- 🎛️ **إعدادات سهلة**: واجهة بسيطة لإدارة الأسرار
- 🔄 **بحث ذكي**: العثور على التوكن في جميع التبويبات

## 📁 ملفات الإضافة

### ملفات الامتداد (في مجلد dist):
- `manifest.json` - ملف البيان
- `background.js` - الخدمة في الخلفية
- `content_bridge.js` - جسر التواصل مع الموقع
- `content_youtube.js` - مراقبة يوتيوب
- `popup.html/js` - واجهة الإعدادات
- `website_auth_bridge.js` - جسر الموقع

### ملفات التطوير:
- `webpack.config.js` - إعدادات البناء
- `package.json` - التبعيات

## 🛠️ التثبيت والإعداد

### 1. تحميل الإضافة
```
افتح chrome://extensions
فعل وضع المطور
انقر "تحميل غير مضغوط"
اختر مجلد dist
```

### 2. إعداد مفتاح السر
```
انقر على أيقونة الإضافة
اضغط "توليد جديد" أو أدخل مفتاحك
اضغط "حفظ"
```

### 3. إضافة جسر الموقع
```html
<!-- أضف هذا في موقعك -->
<script src="path/to/website_auth_bridge.js"></script>
```

```javascript
// في كود تسجيل الدخول
import { onAuthStateChanged } from 'firebase/auth';

onAuthStateChanged(auth, async (user) => {
    if (user) {
        const token = await user.getIdToken();
        window.ViewLoopAuthBridge.sendAuthTokenToExtension(token);
    }
});
```

## 🔧 الاستخدام

1. **افتح موقع ViewLoop** و سجل الدخول
2. **افتح يوتيوب** و ابدأ مشاهدة فيديو
3. **ستبدأ الإضافة تلقائياً** بتتبع المشاهدة
4. **ستحصل على النقاط** عند انتهاء الفيديو

## 🔍 استكشاف الأخطاء

### فحص Console:
```javascript
// في موقع ViewLoop
typeof chrome !== 'undefined' && chrome.runtime

// في يوتيوب
// افتح DevTools وتحقق من الرسائل
```

### فحص التبويبات:
- اضغط F12 في أي صفحة
- اذهب لـ Console
- ستجد رسائل تفصيلية عن حالة الإضافة

### إعادة توليد المفتاح:
- اذهب لإعدادات الإضافة
- اضغط "توليد جديد"
- احفظ المفتاح الجديد

## 🔐 الأمان

- **تشفير مزدوج**: سر الامتداد + توكن المستخدم
- **تحقق المصدر**: فقط الامتداد المصرح يمكنه الوصول
- **تخزين آمن**: chrome.storage مع تشفير
- **حماية من التلاعب**: فحص سرعة الفيديو والإعلانات

## 📊 API Endpoints

```
POST /api/start-session
POST /api/heartbeat
POST /api/complete
```

## 🆔 معرف الامتداد
```
djpncieodekmbejocghjhldjhkcaalic
```

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من Console للرسائل
2. أعد توليد مفتاح السر
3. تأكد من تسجيل الدخول في الموقع
4. تحقق من أن الجسر محقون في الموقع

---

**تم تطوير هذه الإضافة بأمان وكفاءة عالية** 🚀
